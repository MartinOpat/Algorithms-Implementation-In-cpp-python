#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define ull unsigned ll
#define psh push_back

#define PRINT_VEC(v) for (int i = 0; i < v.size(); i++) cout << v[i] << (i < v.size()-1 ? " " : "\n")

ll mod = 1000000007;


int main() {

    ios::sync_with_stdio(false);
    cin.tie(NULL);

    vector<int> test = {1, 2, 3, 4, 5};
    PRINT_VEC(test);

    // int c, n;
    // while (cin >> c >> n) {
    //     vector<pair<int, int>> objs;

    //     for (int i = 0; i < n; i++) {
    //         cin >> objs[i].first;
    //         cin >> objs[i].second;
    //     }
        


    // }

    return 0;
}